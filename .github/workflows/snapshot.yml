name: Snapshot Build
run-name: Snapshot ${{ inputs.snapshot_label }} from ${{ github.ref_name }}

on:
  workflow_dispatch:
    inputs:
      snapshot_label:
        description: "Short label for the snapshot artifact, such as 0.2.2-snapshot-1. Use letters, numbers, dots, and hyphens."
        required: true
        default: "manual"
        type: string
      retention_days:
        description: "Artifact retention in days"
        required: true
        default: "14"
        type: choice
        options:
          - "3"
          - "7"
          - "14"
          - "30"

permissions:
  contents: read

jobs:
  snapshot:
    name: Package Snapshot
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate snapshot inputs
        env:
          SNAPSHOT_LABEL: ${{ inputs.snapshot_label }}
        run: |
          case "$SNAPSHOT_LABEL" in
            *[!A-Za-z0-9.-]*)
              echo "Snapshot label may only contain letters, numbers, dots, and hyphens." >&2
              exit 1
              ;;
          esac

      - name: Typecheck
        run: npm run typecheck

      - name: Build Chromium
        run: npm run build:chromium

      - name: Build Firefox
        run: npm run build:firefox

      - name: Verify update checks
        run: npm run verify:update-check

      - name: Verify URL allowlist
        run: npm run verify:url-allowlist

      - name: Verify platform
        run: npm run verify:platform

      - name: Verify extension smoke
        run: npm run verify:smoke:020

      - name: Verify app smoke
        run: npm run verify:app-smoke:020

      - name: Package release archives
        run: npm run package:release

      - name: Upload snapshot artifacts
        uses: actions/upload-artifact@v4
        with:
          name: milXdy-snapshot-${{ inputs.snapshot_label }}-${{ github.run_number }}-${{ github.sha }}
          path: |
            release/*.zip
            release/*.sha256
          retention-days: ${{ inputs.retention_days }}

      - name: Write snapshot summary
        env:
          SNAPSHOT_LABEL: ${{ inputs.snapshot_label }}
          SOURCE_REF: ${{ github.ref_name }}
          SOURCE_COMMIT: ${{ github.sha }}
          RETENTION_DAYS: ${{ inputs.retention_days }}
        run: |
          {
            echo "## milXdy Snapshot Build"
            echo ""
            echo "- Label: $SNAPSHOT_LABEL"
            echo "- Source ref: $SOURCE_REF"
            echo "- Source commit: $SOURCE_COMMIT"
            echo "- Retention: $RETENTION_DAYS days"
            echo ""
            echo "Snapshot artifacts are temporary manual test builds. They are not GitHub Releases and are not used by the normal updater."
          } >> "$GITHUB_STEP_SUMMARY"
