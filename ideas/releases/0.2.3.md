# milXdy 0.2.3 Private Release Notes

Status: target.

Public theme: Postreader companion voice experiments.

## Labels

- `postreader`
- `integration`
- `experimental`

## Candidate Features

### Postreader TTS side package

Labels: `postreader`, `integration`, `experimental`.

Goal:

- Better local voices without bundling heavy TTS models into the extension.

Possible shape:

- Separate local server/package.
- milXdy links to install docs.
- Postreader uses existing custom HTTP endpoint settings.
- Endpoint supports optional timing boundaries.

## X Post Draft

```text
milXdy 0.2.3 is planned around Postreader voice experiments: investigating a companion local TTS package while keeping browser Web Speech as the default fallback.
```

## Four Screenshot Instructions

1. TTS side-package setup/health state.
2. Postreader custom TTS controls.
3. Playback with custom TTS active.
4. Error/fallback state when the local service is unavailable.
