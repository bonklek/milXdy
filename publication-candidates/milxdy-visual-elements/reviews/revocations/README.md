# Upstream removal advisories

A removed upstream hash remains resolvable. Add an immutable advisory that identifies
the affected catalog ID/version/hash, reason code, impact, safe migration or
replacement when known, and publication date.

This process applies to published default/upstream versions, not private local
files that an author controls directly.
