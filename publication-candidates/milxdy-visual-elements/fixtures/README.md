# Validator fixtures

Everything below this directory is synthetic, non-production test data. Fixture
IDs begin with `fixture.` and source files are abstract code-native examples.

The production catalog and manifests are forbidden from referencing this
directory. Valid fixtures cover both a user-controlled local lane and the
stricter upstream/default lane. They are not real provenance claims, review
decisions, or reusable catalog assets.
