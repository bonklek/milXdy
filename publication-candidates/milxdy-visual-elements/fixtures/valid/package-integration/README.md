# Package integration fixture

This is a schema fixture, not an installable package. It shows the current
`milxdy.app.json` shipping declaration beside an optional provenance lockfile
without adding unsupported fields to the App SDK manifest. The fixture uses an
upstream/default VPL asset so the strict lane remains covered; local custom
packages may instead record an unknown or non-VPL license without project review.
